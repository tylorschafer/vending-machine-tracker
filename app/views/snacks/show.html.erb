<h1>Name: <%= @snack.name %></h1>
<p>Price: $<%= @snack.price %></p>
<p>Vending Locations:</p>
<ul>
  <% @snack.machines.each do |machine| %>
      <li><%= machine.location %> (<%= machine.snacks.count %> kinds of snacks, average price of $<%= machine.average_snack_price %>)</li>
  <% end %>
</ul>
